class Bowling {
    constructor(rolls) {
        this.rolls = rolls;
    }

    score = function() {
        let score;
        for (let i = 0; i < this.rolls.length; i++) {
            if (rolls[i] === 10) {
            }
        }

        return score;
    };

    roll = function(pins) {};
}

module.exports = Bowling;
